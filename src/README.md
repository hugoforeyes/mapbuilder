# Source Overview

React/Vite client for the map builder.

Top files/folders:
- `App.tsx`: main state container (tools, layers, undo/redo, selection) and wiring to canvas/panels.
- `components/`: UI widgets (canvas, toolbar, option panels, sidebars) and their subfolders.
- `assets/`: asset metadata JSON used to populate the catalog.
